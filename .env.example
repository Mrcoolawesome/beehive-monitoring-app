# Copy this file to .env and fill in real values. See README.md's Setup
# section for how to create the Postgres role/database referenced below.

# Connection string for the local Postgres database.
DATABASE_URL="postgresql://beehive_app:CHANGE_ME@localhost:5432/beehive?schema=public"

# Directory the F' downlink (or a script relaying it) drops WeightSessionRecord
# JSON files into. The watcher (scripts/watcher.ts) creates this directory,
# plus processed/ and failed/ subfolders inside it, automatically.
WATCH_DIR="./data/incoming"

# Identifies which Raspberry Pi / hive a reading came from. There's only one
# Pi today and the F' JSON payload itself doesn't carry a MAC address, so
# this is a single hardcoded value rather than something parsed per file.
# Replace with the real Pi's MAC address before running the watcher for real.
PI_MAC_ADDRESS="AA:BB:CC:DD:EE:FF"
